# [Delaney] Codebase audit — delaney — 2026-06-10

> Phase A of the AUDIT-AND-REFRESH-ARCH composite (`all` run, delaney leg). Read-only. Sources walked in order: (1) `_cowork/dashboard-delaney.json` (kernel-side, kernel-schema-v1.14; **0 active rows** — DM1–DM11 all graduated to `_cowork/milestones-done/`), (2) `_cowork/working-agreement.md` (kernel canonical via the rendered AGENTS.md stub), (3) recent runs — 8 DM logs in the mount's `_cowork/runs/` + DM4/DM5/DM10 kernel-side; DM11's close-log read in depth, (4) the source tree (`router/` shell + confs, `windows/` PowerShell action scripts, `windows-ui/KidBlockUI/` WPF app). Project: **KidBlock** — scheduled internet control for an EdgeRouter Pro 8; delaney HEAD `30a0c85` (DM11), 12 commits since 2026-06-02.

## Findings

### 1. Dead / orphaned code

- Superseded-but-retained notebook scripts in `windows/`: DM2's row explicitly "replaces `Edit-Schedule.ps1`" and DM4 replaced the hand-edit domain-blocklist flow, yet `windows/Edit-Schedule.ps1`, `windows/Edit-Devices.ps1` and the related hand-edit path remain on disk beside the KidBlockUI app that superseded them. The original design framed the notebook side as convenience fallback — a deliberate keep-or-retire disposition is still owed.
- `windows-ui/.vs/`, `bin/`, `obj/` build residue present on disk but correctly gitignored (`.gitignore` covers all three) — hygiene only, not tracked debt.

### 2. Logging strategy consistency

- Router-side: single consistent sink — `kidblock.sh`'s `log()` helper appends timestamped lines to `/var/log/kidblock.log` (`router/kidblock.sh:54`); DM5 streams it into the UI's LogTail panel and DM10 added parsed DNS-query kinds via dnsmasq `log-queries`.
- KidBlockUI has **no app-side log sink**: `KidBlockUI.csproj` carries only SSH.NET + CommunityToolkit.Mvvm (the `Microsoft.Extensions.Logging.Abstractions` DLL in `bin/` is transitive). Failures surface in-UI only; a one-file rolling log would help post-hoc debugging of SSH/apply failures. Minor.
- `windows/` PowerShell action scripts print interactively — appropriate for their shape. (no further findings)

### 3. Architectural drift vs north-star

- **`current_focus` misrepresents the project's lifecycle stage**: it still reads "Project bootstrap — no milestones yet. Owner pins the first work via INGEST-IDEA" while DM1–DM11 shipped a complete product arc (SSH client + config parsers + read-only window → schedule timeline → per-device overrides → domain blocklist UI → live log tail → whitelist mode → DHCP-lease fetch → layout rework → override end-to-end → DNS observability → allow-duration SplitButtons). The kernel-side WBS is the single source of truth and it describes an empty project.
- The 2026-06-02 arch snapshot predates the entire KidBlockUI program — the project's largest structural change (a second process boundary + a second SSH path) was undocumented until this run's Phase B.

### 4. UX inconsistencies

- (no findings) — single-author WPF app, freshly layout-reworked by DM8; no divergent affordances evidenced in the tree walk.

### 5. Dependency-view confusion in dashboard-delaney.json

- Top-level `PARKING` key still present at schema v1.14 (retired kernel-side at v1.17/v1.18) — rides the cross-project migrate sweep.
- Phase scaffold P1–P4 carries empty `children` arrays with owner-edit taglines — consistent with the graduate-out model, but combined with the bootstrap `current_focus` the dashboard reads as never-started rather than version-1-complete.
- Run-log home split: DM4/DM5/DM10 logs live kernel-side, DM1–DM3/DM6–DM9/DM11 in the mount, and DM3's log exists in **both** (the mount copy sits modified-uncommitted alongside DM6/DM8) — same two-homes pattern flagged in the kernel audit.

## Worth attention

1. **Refresh `current_focus`** — from bootstrap stub to "KidBlock v1 complete (DM1–DM11); next wave unscoped" or whatever Adam intends next; the current text actively misleads.
2. **Disposition the superseded `windows/` scripts** — keep-as-documented-fallback or retire `Edit-Schedule.ps1` / `Edit-Devices.ps1` now that KidBlockUI owns those flows.
3. **Commit the mount's modified run-logs** (DM3/DM6/DM8 residue) per the solo-dev fold convention.
4. **PARKING key + v1.14 → v1.18 migration** — kernel-driven sweep (cross-ref kernel audit Worth-attention #4).

## Park-worthy

- KidBlockUI one-file rolling log sink (SSH/apply failure forensics).
- `appsettings.json` `KeyPath` carries a literal `%USERPROFILE%` token — works only because `RouterClient` expands it; worth a comment in the file so a hand-editor doesn't paste an absolute path with different expansion expectations.

## No-action

- `kidblock-overrides.conf` absent from the repo — by design: runtime state created and pruned on the router by `kidblock.sh` (DM9); the repo carries the script that manages it.
- Kernel-side `_dm11_*.py/json` scratch — DM11's close documents the leave-as-untracked precedent (`_dm1_to_dm6/_dm8/_dm9`); captured estate-wide in the kernel audit's runs-hygiene finding instead.
- `Wi-Fi-Schedule.pdf` at repo root — owner reference document, not code.

<!-- eof: codebase-audit-report-v1.0 -->
