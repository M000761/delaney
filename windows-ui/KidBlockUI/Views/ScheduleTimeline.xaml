<UserControl x:Class="KidBlockUI.Views.ScheduleTimeline"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent"
             MinHeight="120">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Background="#1E1E1E"
                BorderBrush="#3F3F46" BorderThickness="1,1,0,1"
                ClipToBounds="True">
            <Canvas x:Name="ProfileLabels"
                    Background="Transparent"
                    SnapsToDevicePixels="True"
                    SizeChanged="ProfileLabels_SizeChanged"
                    MinWidth="80"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"/>
        </Border>

        <Border Grid.Row="0" Grid.Column="1" Background="#163B16"
                BorderBrush="#3F3F46" BorderThickness="1"
                ClipToBounds="True">
            <Canvas x:Name="TimelineCanvas"
                    Background="Transparent"
                    SnapsToDevicePixels="True"
                    MouseLeftButtonDown="TimelineCanvas_MouseLeftButtonDown"
                    SizeChanged="TimelineCanvas_SizeChanged"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"/>
        </Border>

        <Canvas Grid.Row="1" Grid.Column="1" x:Name="HourAxis"
                Background="#1E1E1E"
                ClipToBounds="True"
                SizeChanged="HourAxis_SizeChanged"/>
    </Grid>
</UserControl>
